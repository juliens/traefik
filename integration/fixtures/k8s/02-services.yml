kind: Deployment
apiVersion: apps/v1
metadata:
  name: whoami
  namespace: default
  labels:
    app: traefiklabs
    name: whoami

spec:
  replicas: 2
  selector:
    matchLabels:
      app: traefiklabs
      task: whoami
  template:
    metadata:
      labels:
        app: traefiklabs
        task: whoami
    spec:
      containers:
        - name: whoami
          image: traefik/whoami
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: whoami
  namespace: default

spec:
  ports:
    - name: http
      port: 80
  selector:
    app: traefiklabs
    task: whoami

---
kind: Deployment
apiVersion: apps/v1
metadata:
  name: invalid
  namespace: default
  labels:
    app: traefiklabs
    name: invalid

spec:
  replicas: 2
  selector:
    matchLabels:
      app: traefiklabs
      task: invalid
  template:
    metadata:
      labels:
        app: traefiklabs
        task: invalid
    spec:
      containers:
        - name: invalid
          image: traefik/invalid
          ports:
            - containerPort: 80

---
apiVersion: v1
kind: Service
metadata:
  name: invalid
  namespace: default

spec:
  ports:
    - name: http
      port: 80
  selector:
    app: traefiklabs
    task: invalid

